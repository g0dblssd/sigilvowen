# Патч №017 — враги больше не проваливаются под арену

Дата: 2026-09-22

## Исправлено

- В маску столкновений `Enemy` возвращён слой `World`. После перехода на
  `CharacterBody3D` враг имел гравитацию, но не видел коллизию пола и падал вниз.
- Ближняя атака дополнительно проверяет разницу высот между рейдером и игроком.
  Даже при повреждённой геометрии или падении за границу враг не сможет атаковать
  цель через пол.

## Проверка

- `dotnet build Sigilwoven.csproj` — успешно: 0 warnings, 0 errors.
- `godot-mono --headless --path . --quit-after 1800` с отдельными XDG-каталогами
  — exit code 0; ритуал завершён, 4 рейдера созданы, C# / Script Error нет.
